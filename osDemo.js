import os from 'os';

console.log(os.userInfo);// logs information about the current user

console.log(os.totalmem);// logs the total system memory in bytes

console.log(os.freemem);// logs the amount of free system memory in bytes

console.log(os.cpus);// gives an object for every cpu on system
